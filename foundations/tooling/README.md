# Tooling

Starter setup helpers for common terminal/network admin tools.

- `linux_install_network_tools.sh` installs baseline packages frequently used in this repository, including `nmap` and `netcat`.
