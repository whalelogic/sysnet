# IP and MAC Configuration

Foundational scripts for inspecting interface/IP/MAC configuration.

- `linux_show_ip_mac.sh` lists interface, IPv4, and MAC data on Linux.
- `windows_show_ip_mac.ps1` lists adapter, IPv4, and MAC data on Windows.
